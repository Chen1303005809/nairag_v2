# RAG 知识库领域

本文件定义系统内统一使用的业务术语。它只描述领域概念，不承担数据库或接口设计职责。

## 知识结构

**父类（Parent）**：
全局的问题大类，包含问题大类名称、主关键词及词法规则；它不隶属于任何知识库。
_Avoid_：Collection 父类、知识库父类、可独立发布的父条目

**子条目（Child）**：
父类之下的具体知识单元；正式创建或提交时必须属于一个已经存在且可选择的父类。知识草稿不是子条目，因此草稿阶段可以尚未归类。
_Avoid_：分片、父条目版本、Milvus 记录

**主子条目（Primary Child）**：
每个父类唯一且永久存在的特殊子条目，用来表达父类本身代表的知识；它随父类原子创建，不能删除或迁移到其他父类。
_Avoid_：默认命中、虚拟条目、父类摘要

**普通子条目（Ordinary Child）**：
除主子条目以外的子条目，描述父类下更具体的问题与回答。

**修订（Revision）**：
父类或子条目的一份不可变内容快照；修改已发布内容意味着创建新修订，而不是覆盖旧修订。
_Avoid_：就地修改、可变版本

**父类—主子条目聚合（Parent–Primary Aggregate）**：
父类及其唯一主子条目组成的完整审核单元，两者始终一起创建、修订和审核。

**佐证附件（Evidence Attachment）**：
绑定到某个不可变子条目修订、随检索结果对外展示的佐证材料；它不是知识检索的向量来源。
_Avoid_：图片知识、附件分片、附件向量

## 草稿与智能处理

**知识草稿（Knowledge Draft）**：
新增普通子条目提交审核前的、可变且仅创建者可见的暂存内容；它可以不完整或尚未归类，不是子条目、修订或审核提交。
_Avoid_：候选修订、待审核知识、可变审核提交

**智能上传批次（Intelligent Ingestion Batch）**：
从一个标准化客户会话异步提炼零条或多条智能生成草稿的一次处理；批次本身不是投稿。
_Avoid_：批量投稿、自动审核、聊天记录归档

**标准化会话（Normalized Conversation）**：
渠道适配器从单个客户会话整理出的结构化消息序列，明确表达说话人、角色和正文，供智能上传或快速检索使用。
_Avoid_：企业微信原始文本、LLM 自由解析文本

**快速检索（Conversation-assisted Retrieval）**：
从一个标准化会话同步提炼待查询问题并检索现有知识的过程；LLM 只生成查询，不生成最终答案。
_Avoid_：智能回答、对话归档、自动回复

## 知识库与发布

**知识库（Knowledge Base）**：
业务侧可管理、可分配审查管理员的知识集合；普通用户可以查询并向所有知识库投稿。
_Avoid_：组织、租户

**物理 Collection 代（Physical Collection Generation）**：
某个知识库当前采用的一代物理检索索引；模型升级时会创建新一代并在验证后切换。
_Avoid_：父类 Collection、永久 Collection 名称

**投放（Targeting）**：
一次修订被提交到一个或多个知识库接受独立审核的关系。
_Avoid_：父类绑定知识库、逗号分隔的 collections 字段

**发布关系（Publication）**：
一个子条目在一个知识库中的发布状态及当前生效修订；同一子条目在不同知识库可以生效不同修订。
_Avoid_：子条目全局状态、父类发布状态

**当前修订（Active Revision）**：
某个发布关系当前对检索用户可见的已发布修订；新修订失败或待审时仍由旧修订继续提供服务。

## 审核

**审核提交（Review Submission）**：
一次明确提交给指定知识库审查管理员的不可变候选修订。

**审核决定（Review Decision）**：
某知识库的一名获授权审查管理员对候选修订作出的通过或驳回结论。

**父类全局一致发布（Globally Consistent Parent Publication）**：
父类—主子条目聚合的新修订只有在其全部目标知识库均通过并准备就绪后才整体生效，不能按知识库展示不同的父类内容。

## 检索

**向量分片（Vector Fragment）**：
从一个子条目修订的可检索字段确定性生成的最小召回单元；多个向量分片可以共同指向同一子条目修订。
_Avoid_：子条目、命中结果

**父类关键词保底（Parent Keyword Fallback）**：
查询命中父类主关键词或受控词法规则时，保证该父类的主子条目进入结果的业务规则。
_Avoid_：语义召回、硬编码回答

**有用反馈（Helpful Feedback）**：
用户在一次检索后明确选择真正有用的子条目并提交的事件。
_Avoid_：点击量、曝光量、原始 hit_count

**有用次数（Helpful Count）**：
按“子条目 × 知识库”汇总的有用反馈次数，用作受限排序加成，而不是相关性的替代品。

**无匹配（No Match）**：
没有候选达到相关性阈值且没有触发父类关键词保底时返回的明确结果状态。

## 角色

**系统管理员（System Administrator）**：
管理账号、知识库、角色权限以及审查管理员与知识库关系的角色。

**审查管理员（Review Administrator）**：
继承普通用户能力，并且只能审核获分配知识库投稿的角色。

**普通用户（Normal User）**：
可以查询全部知识库并向任意知识库投稿的角色。
