# Keep these values aligned with the deployment's backend LLM limits. They are
# evaluated when the Vite bundle is built and let the browser reject oversized
# pasted conversations before it sends a request.
VITE_LLM_MAX_CONVERSATION_MESSAGES=200
VITE_LLM_MAX_CONVERSATION_CHARS=30000
